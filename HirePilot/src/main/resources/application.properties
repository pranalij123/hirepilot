spring.application.name=HirePilot


spring.datasource.url=jdbc:postgresql://localhost:5432/Erp_HirePilot
spring.datasource.username=postgres
spring.datasource.password=pranalI@30

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect



# Format the SQL nicely in logs
spring.jpa.properties.hibernate.format_sql=true
server.port=8080


# === Email Configuration ===
spring.mail.host=erp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_ADDRESS}
spring.mail.password=${EMAIL_APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=erp.gmail.com


# =======Actuator Configuration =====================
# Enable specific endpoints only (alternative to '*')
# management.endpoints.web.exposure.include=health,info,metrics,loggers

management.endpoints.web.exposure.include=*
# Show full health details (useful for debugging)
management.endpoint.health.show-details=always

# Customize the actuator base path (default is /actuator)
# management.endpoints.web.base-path=/manage


# Enable shutdown endpoint explicitly (disabled by default)
management.endpoint.shutdown.enabled=true


# File upload/download config
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

security.jwt.secret-key=3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
# 1h in millisecond
security.jwt.expiration-time=3600000

